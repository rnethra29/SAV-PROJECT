# 02-site / 03 — Subcontractor Management

**Status: not started.**

No backend, frontend, or database work exists for this sub-module yet. This folder is a structural placeholder only — reserving its position in the numbering (`03-`) ahead of the sub-modules that still need names (`04`–`10`) — and intentionally contains no code, no fake endpoints, no placeholder models, and no dummy UI.

When this sub-module's design phase begins, follow the same module-development sequence used for the rest of the ERP (see `SAV-ERP-PROJECT-CONTEXT.md` §E): requirements → workflow → entities → relationships → RBAC → API → frontend → Excel import/export → reports → notifications → approvals → audit → integrations → edge cases → acceptance criteria → approval → *then* implementation.
